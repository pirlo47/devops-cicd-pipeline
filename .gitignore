# Dependencies
node_modules/
package-lock.json # Optional: keep this if you want to lock versions, exclude if you rely strictly on package.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables (CRITICAL FOR SECURITY)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE settings (Optional but recommended)
.vscode/
.idea/

# Build outputs (Future proofing)
dist/
build/
coverage/